import { LabelsByCategoryPipe } from './labels-by-category.pipe';

describe('LabelsByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelsByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
